[build]
  # No build command required if `dist` is already committed; Netlify will just publish the folder
  command = ""
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
